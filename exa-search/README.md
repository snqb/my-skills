# exa-search — Semantic Web Search via Exa AI

AI-native search with 5 endpoints: `/search` (semantic/deep), `/findSimilar`, `/contents`, `/answer`, `/research` (async agent-style). Supports structured output, content extraction, date/domain filters, and category filtering.

Requires `EXA_API_KEY`.
