# ticket — Git-Native Task Tracking

Fast, local-first issue tracking as Markdown files in `.tickets/`. Maintains state across sessions, tracks dependencies, provides a clear frontier of work for coding agents.
