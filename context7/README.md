# context7 — Live Library Documentation

Fetch real-time, version-specific docs for fast-moving libraries (Aiogram, LangChain, Next.js, Supabase, etc.) via MCP. Prevents hallucinated APIs and deprecated method calls.

**Rule**: always use before generating code for libraries that update frequently.
