# deno — TypeScript Scripting with dax

Single-file TypeScript scripts with dax (shell), `@std` (FP/collections/async), zero setup. Default runtime for all generated scripts — replaces bash and Python one-offs.

```typescript
#!/usr/bin/env -S deno run --allow-all
```
