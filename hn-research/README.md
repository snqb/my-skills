# hn-research — Hacker News Intelligence

Research HN via Algolia API. Stories + comments for full sentiment analysis. Filter by recency, popularity, tags (`story`, `comment`, `show_hn`, `ask_hn`, `front_page`), and numeric filters (`points>100`).

No API key needed — free public API.
