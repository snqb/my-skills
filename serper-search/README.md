# serper-search — Google SERP as JSON

Fast, cheap Google search via Serper.dev. Endpoints: web, news, images, videos, places, shopping, scholar, autocomplete. Time filters, localization, batch queries. ~$0.30–1.00 per 1k queries.

Requires `SERPER_API_KEY`.
