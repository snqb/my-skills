# tmux — Background Process Management

Spawn and manage long-running processes in tmux panes — dev servers, builds, logs, watchers. Single session `pi` with named windows per task. Never block the main thread.
